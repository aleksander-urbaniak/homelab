@pve-node01

# network interface settings; autogenerated
# Please do NOT modify this file directly, unless you know what
# you're doing.
#
# If you want to manage parts of the network configuration manually,
# please utilize the 'source' or 'source-directory' directives to do
# so.
# PVE will preserve these directives, but will NOT read its network
# configuration from sourced files, so do not attempt to move any of
# the PVE managed interfaces into external files!

auto lo
iface lo inet loopback

iface nic0 inet manual

auto enxceph01
iface enxceph01 inet static
        address 198.51.100.11/24
        mtu 1500
#post-up /sbin/ethtool -K enxceph01 rx off tx off sg off || true

auto vmbr0
iface vmbr0 inet static
        address 192.0.2.11/24
        gateway 192.0.2.1
        bridge-ports nic0
        bridge-stp off
        bridge-fd 0

source /etc/network/interfaces.d/*



@pve-node02

# network interface settings; autogenerated
# Please do NOT modify this file directly, unless you know what
# you're doing.
#
# If you want to manage parts of the network configuration manually,
# please utilize the 'source' or 'source-directory' directives to do
# so.
# PVE will preserve these directives, but will NOT read its network
# configuration from sourced files, so do not attempt to move any of
# the PVE managed interfaces into external files!

auto lo
iface lo inet loopback

iface nic0 inet manual

auto enxceph02
iface enxceph02 inet static
        address 198.51.100.12/24
        mtu 1500

auto vmbr0
iface vmbr0 inet static
        address 192.0.2.12/24
        gateway 192.0.2.1
        bridge-ports nic0
        bridge-stp off
        bridge-fd 0

source /etc/network/interfaces.d/*



@pve-node03

# network interface settings; autogenerated
# Please do NOT modify this file directly, unless you know what
# you're doing.
#
# If you want to manage parts of the network configuration manually,
# please utilize the 'source' or 'source-directory' directives to do
# so.
# PVE will preserve these directives, but will NOT read its network
# configuration from sourced files, so do not attempt to move any of
# the PVE managed interfaces into external files!

auto lo
iface lo inet loopback

iface nic0 inet manual

auto enxceph03
iface enxceph03 inet static
        address 198.51.100.13/24
        mtu 1500
#post-up /sbin/ethtool -K enxceph03 rx off tx off sg off || true

auto vmbr0
iface vmbr0 inet static
        address 192.0.2.13/24
        gateway 192.0.2.1
        bridge-ports nic0
        bridge-stp off
        bridge-fd 0

source /etc/network/interfaces.d/*
