# 📦 Portainer Agent (Docker) ✨

This folder contains a Docker Compose setup for the Portainer agent used on standalone Docker hosts.

## Files

- `compose.yml`: Portainer agent stack

## Deploy

Run from this folder:

```bash
docker compose -f compose.yml up -d
```
