apiVersion: v1
kind: Secret
metadata:
  name: nebula-sync-secret
  namespace: nebula-sync
type: Opaque
stringData:
  # Fill in your endpoints and passwords. If you use HTTPS with IPs, keep CLIENT_SKIP_TLS_VERIFICATION=true in the Deployment.
  PRIMARY: "REPLACE_ME"
  REPLICAS: "REPLACE_ME"

