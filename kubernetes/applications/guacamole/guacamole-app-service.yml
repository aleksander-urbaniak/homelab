apiVersion: v1
kind: Service
metadata:
  name: guacamole-app
  namespace: guacamole
  labels:
    app: guacamole
spec:
  type: ClusterIP
  selector:
    app: guacamole
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080

