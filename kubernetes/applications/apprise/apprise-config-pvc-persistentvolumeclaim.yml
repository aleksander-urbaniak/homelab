apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apprise-config-pvc
  namespace: apprise
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

# =====================================
# 2. Apprise Deployment
# =====================================
