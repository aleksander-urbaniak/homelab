apiVersion: v1
kind: Service
metadata:
  name: pihole-exporter
  namespace: pihole-exporter
  labels:
    app.kubernetes.io/name: pihole-exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pihole-exporter
  ports:
    - name: http-metrics
      port: 9617
      targetPort: metrics
      protocol: TCP
