apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pulze-dashboard-data-pvc
  namespace: pulze-dashboard
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
