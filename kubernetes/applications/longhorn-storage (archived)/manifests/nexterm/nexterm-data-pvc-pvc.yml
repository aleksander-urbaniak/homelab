apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nexterm-data-pvc
  namespace: nexterm
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
