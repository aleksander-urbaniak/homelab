apiVersion: v1
kind: Service
metadata:
  name: pulse
  namespace: pulse
  labels:
    app: pulse
spec:
  type: ClusterIP
  selector:
    app: pulse
  ports:
    - name: http
      protocol: TCP
      port: 7655
      targetPort: 7655
