apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: zabbix-web-ssl
  namespace: zabbix
spec:
  storageClassName: longhorn
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi

